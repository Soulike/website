{
    "name": "@website/request",
    "main": "dist/index.js",
    "dependencies": {
        "@website/classes": "workspace:^",
        "assert": "^2.0.0",
        "typescript": "^5.0.2"
    },
    "scripts": {
        "install": "pnpm build",
        "build": "rm -rf dist && tsc"
    },
    "devDependencies": {
        "@types/assert": "^1.5.6"
    }
}
