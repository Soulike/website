# Reusable Libraries

Universal, business-agnostic packages that can be extracted and reused across different projects.

- `web/` - Utilities and helpers for web apps only.
- `server/` - Utilities and helpers for Node.js server apps only.
- `universal/` - Utilities and helpers usable to all types of apps.
