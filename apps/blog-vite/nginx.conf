server {
  listen 3000;
  server_name blog;
  location / {
    root /blog-dist;
    try_files $uri $uri/ /index.html;
  }
  location /assets {
    root /blog-dist/assets
    add_header Cache-Control "public, immutable";
    expires 1y;
  }
  error_page 404 /index.html;
}
