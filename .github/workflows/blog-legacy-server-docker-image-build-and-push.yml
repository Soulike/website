name: Blog Legacy Server Docker Image Build and Push

on:
  push:
    branches:
      - 'main'
    paths:
      - 'apps/server/blog-legacy/**'
      - 'modules/**'
      - 'libraries/**'
      - 'bun.lock'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/docker-image-build-and-push.yml
    secrets: inherit
    with:
      docker-file-path: ./apps/server/blog-legacy/Dockerfile
      docker-build-target: blog-server-legacy
      push-to-registry: true
